<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.oaklandstudent.medicalid">

    <uses-permission android:name="android.permission.NFC" />

    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="true" />

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<activity android:name=".Questions"></activity>-->
        <activity android:name="edu.oaklandstudent.medicalid.PersonalInformation"></activity>
        <activity android:name="edu.oaklandstudent.medicalid.EmergencyContacts"></activity>
        <activity android:name="edu.oaklandstudent.medicalid.MedicalConditions"></activity>
        <activity android:name="edu.oaklandstudent.medicalid.AccidentInformation"></activity>
        <activity android:name="edu.oaklandstudent.medicalid.authFailed"></activity>
        <activity android:name="edu.oaklandstudent.medicalid.password"></activity>
        <activity android:name="edu.oaklandstudent.medicalid.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="edu.oaklandstudent.medicalid.NFC.HostCardEmulatorService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>

            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>
    </application>

</manifest>